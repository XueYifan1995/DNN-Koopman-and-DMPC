# DNN-Koopman-and-DMPC
This code constructs a dynamic model for the carrier by employing the Koopman operator encoded via deep neural networks, and then implements robust tube mpc control based on the modeling residuals.
